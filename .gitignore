# ============================================================
# 本仓库 .gitignore
# 说明：本仓库是「素拓展部」集中学习素材库，成品以 .html + .md + 图片为主，
#       因此忽略项主要针对：操作系统/编辑器产生的临时文件、依赖缓存、
#       大型原始素材（视频/工程文件等）以及 AI 制作过程中产生的中间产物。
# ============================================================

# ---------- 操作系统临时文件 ----------
# Windows 资源管理器 / 临时文件
Thumbs.db
desktop.ini

# macOS 访达临时文件
.DS_Store

# 通用临时 / 备份文件
*.tmp
*.temp
*~
*.bak

# ---------- 编辑器 / IDE 缓存 ----------
# VS Code
.vscode/
!.vscode/extensions.json

# JetBrains（含 WebStorm / IDEA）
.idea/

# 其他编辑器
*.sublime-project
*.sublime-workspace

# ---------- 依赖与构建产物 ----------
# 如某期项目引入前端依赖（npm）
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 构建输出目录
dist/
build/
out/

# 打包产物
*.zip
*.tar
*.tar.gz

# ---------- 大型原始素材（不进版本库，避免仓库膨胀） ----------
# 视频 / 音频原始素材
*.mp4
*.mov
*.avi
*.mkv
*.wmv
*.flv
*.webm
*.mp3
*.wav
*.flac

# 剪辑 / 设计 / 3D / 建模 等软件工程文件（体积大且非通用格式）
*.prproj
*.prelx
*.frx
*.blend
*.max
*.ma
*.psd
*.ai
*.fig
*.sketch

# 大型图片素材（如需入库请单独确认，此处默认忽略）
*.psb
*.raw
*.cr2
*.nef

# ---------- AI 制作过程中的中间 / 缓存文件 ----------
# 本地构建缓存
.cache/
__pycache__/
*.pyc

# ---------- 环境 / 密钥（安全） ----------
.env
.env.*
*.key
*.pem
*.p12

# ---------- 本地个人配置（不共享） ----------
# 例如个人笔记 / 草稿，不想提交的本地文件可放到 ignored/ 目录
local-notes/
.draft/

# AI 行为指南：仅本机个人约定，不进入版本库、不共享给团队 / 克隆者
CLAUDE.md
